export const cloneSession = (completedSession) => {
    const nextSession =   JSON.parse(JSON.stringify(completedSession));
    changeArray = [exerciseNumber, totalSets, groupNumber, workTime, restTime]
}